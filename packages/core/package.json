{
  "name": "@led/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "LED 電光掲示板 — 純粋ドメインロジック (no DOM, no canvas)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./assets/k8x12.bin": "./assets/k8x12.bin",
    "./assets/misaki.bin": "./assets/misaki.bin"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "mutation": "echo 'Stryker config TBD in M2'"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.4.0",
    "fast-check": "^4.6.0"
  }
}
